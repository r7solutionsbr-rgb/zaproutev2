# --- DATABASE ---
DATABASE_URL="postgresql://user:password@localhost:5432/zaproute?schema=public"

# --- AUTH ---
JWT_SECRET="seu_segredo_super_seguro"
ZAPI_SECURE_TOKEN="token_seguro_para_webhooks"

# --- STORAGE (Cloudflare R2) ---
# Endpoint: https://<ACCOUNT_ID>.r2.cloudflarestorage.com
AWS_ENDPOINT="https://xxxxxxxx.r2.cloudflarestorage.com"
AWS_ACCESS_KEY_ID="sua_access_key_r2"
AWS_SECRET_ACCESS_KEY="sua_secret_key_r2"
AWS_BUCKET="zaproute-bucket"
# URL Pública (Custom Domain ou R2.dev)
AWS_PUBLIC_URL="https://pub-xxxx.r2.dev"

# --- EMAIL (AWS SES - SMTP) ---
# Para obter estas credenciais:
# 1. Vá no Console AWS > Amazon SES > SMTP Settings
# 2. Clique em "Create SMTP credentials"
# 3. Copie o Usuário e Senha gerados (NÃO são os mesmos do IAM User padrão)

SMTP_HOST="email-smtp.us-east-2.amazonaws.com"
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER="arn:aws:iam::405531875705:user/suporte"
SMTP_PASS="AKIAV424VLF4TI2VR6G"
EMAIL_FROM='"ZapRoute" <suporte@zaproute.com.br>'
